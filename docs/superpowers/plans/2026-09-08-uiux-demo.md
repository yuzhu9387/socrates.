# Socrates UI/UX Demo Implementation Plan

**Goal:** Deliver a paste-ready English designer prompt and a self-contained, visually polished HTML prototype whose visible navigation and controls work.

**Architecture:** Local browser state shared by dashboard, brainstorm editor, and saved knowledge maps. React Flow provides mature node/edge interactions. Build the complete prototype into one HTML file; no backend or live MCP connection is implied.

**Spec:** /Users/guoyuzhu/Socrates/docs/superpowers/specs/2026-09-08-socrates-system-design.md

**Constraints:** English UI, bilingual original content, monochrome palette, abstract ink background, independent notes/node references, frozen saved map content, all visible buttons meaningful.

## Deliverables and checks

- [x] `docs/uiux-designer-prompt.md`: standalone designer brief and acceptance checklist.
- [x] `demo/src/model.js`: seed data, independent note references, frozen saves, safe deletion, bilingual tag normalization. Verify these behaviors with Node tests.
- [x] `demo/src/main.jsx`: dashboard CRUD/filter/selection, map list/detail, tag management, trash, working drawer/modal and hash navigation.
- [x] `demo/src/board.jsx`: React Flow drag/drop, move, connect, group, undo/redo, zoom, node/edge inspector, new insight, save and preview.
- [x] `demo/src/transfers.js`: real Markdown, DOCX, XLSX downloads and Markdown/JSON import, original language preserved.
- [x] `demo/src/styles.css`: grayscale design tokens, typography, ink texture, responsive layouts, reduced motion.
- [x] Build to `demo/dist/index.html` and copy to `Socrates-UIUX-Demo.html`.
- [x] Browser verification: all routes and core control families, CRUD round trip, filter and selection, source-note versus card deletion, save map then edit, tag reorder, import/export, keyboard guards, local persistence and mobile layout. Native Chinese IME candidate handling is guarded in code; no OS IME automation was claimed. Local file URL launch is blocked by the embedded browser policy; interactions were verified through the local development server.
- [x] Capture dashboard, brainstorm and knowledge-map screenshots; hand off HTML and prompt.

Backend CRUD, authentication, cloud synchronization and live MCP belong to the approved production design. Settings may explain these boundaries but must not present simulated connection success as real.
